# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Project was downgraded from SDK 57 to SDK 56 (2026-08-10): Expo Go for SDK 57
had not yet been approved on the iOS App Store, so SDK 56 is the version that
can actually be tested with Expo Go right now. Re-check before upgrading
again.
