# IP local del PC donde corre backend/probar-local.ps1 (no "localhost": Expo
# Go en el iPhone no resuelve el localhost del PC). Ver la IP en los logs de
# probar-local.ps1, que arranca escuchando en 0.0.0.0 y la muestra.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8090
